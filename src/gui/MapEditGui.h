#pragma once

//-------------------------------------------------
// MapEditGui
//-------------------------------------------------

namespace sg::gui
{
    class MapEditGui
    {
    public:
        //-------------------------------------------------
        // Type
        //-------------------------------------------------

        enum class Action
        {
            RAISE,
            LOWER,
            SET_TRAFFIC
        };

        //-------------------------------------------------
        // Member
        //-------------------------------------------------

        Action action{ Action::RAISE };

        //-------------------------------------------------
        // Logic
        //-------------------------------------------------

        void Render();

    protected:

    private:

    };
}
